apiVersion: v1
kind: Service
metadata:
  name: bluegreen-blue-preview
  namespace: bluegreen-demo
  labels:
    app: bluegreen-demo
    role: preview
    version: blue
spec:
  type: ClusterIP
  selector:
    app: bluegreen-demo
    version: blue
  ports:
    - name: http
      port: 80
      targetPort: 80
