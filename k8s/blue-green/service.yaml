apiVersion: v1
kind: Service
metadata:
  name: bluegreen-demo
  namespace: bluegreen-demo
  labels:
    app: bluegreen-demo
spec:
  type: ClusterIP
  selector:
    app: bluegreen-demo
    version: green
  ports:
    - name: http
      port: 80
      targetPort: 80
