apiVersion: v1
kind: Service
metadata:
  name: security-demo
  namespace: security-demo
  labels:
    app: security-demo
spec:
  type: ClusterIP
  selector:
    app: security-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
